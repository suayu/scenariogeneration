# Scenario-Dreamer 项目工作协议

## 实验与安全

- 修改前检查 `git status --short` 和当前分支；保留用户已有的未跟踪 `logs/` 与 `nl_guidance_eval/`，不得删除、覆盖或纳入提交。
- 先进行小规模冒烟测试，再启动中大规模实验；固定清单、随机种子、模型配置和计算预算，并保存配置快照、日志、JSON/CSV 与完整 MP4。
- 不为提高碰撞率而放宽物理可行性、道路约束或非目标交通安全约束。
- 未经明确要求，不提交 GitHub。

## 中大规模实验监控

- 中大规模实验使用独立后台任务运行，保存 PID、运行日志和产物目录，避免交互式会话中断任务。
- 监控间隔固定为每 **15 分钟**一次；检查任务存活状态、完成场景数、日志错误和 MP4/结果文件数量，不得因常规技术问题停止正在运行的任务。
- 任务完成后自动验证汇总结果与视频完整性，收集指标和代表性成功/失败案例，同步用户可访问的结果文件；如配置可用，发送不含凭据的 QQ HTML 摘要邮件。
- 若发现影响实验有效性的错误，应保留失败日志，修复后使用相同冻结清单重新运行；不得把无效运行作为正式结论。
